{
  "extends": "../config/tsconfig.base",
  "compilerOptions": {
      "outDir": "dist",
      "allowJs": true,
      "target": "es5",
  },
  "include": [
      "src/**/*"
  ],
  "exclude": [
    "build",
    "scripts",
    "acceptance-tests"
  ]
}